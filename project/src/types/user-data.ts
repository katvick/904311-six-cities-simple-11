export type UserData = {
  id: number;
  email: string;
  token: string;
}

export type AuthInfo = {
  avatarUrl: string;
  email: string;
  id: number;
  isPro: boolean;
  name: string;
  token: string;
}
